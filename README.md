# سیستم چت تحت شبکه (Java Socket Chat System)

این پروژه یک سیستم پیام‌رسان کلاینت-سرور مبتنی بر پروتکل TCP است که با استفاده از قابلیت‌های Multi-threading و Socket Programming در زبان جاوا پیاده‌سازی شده است.

## 📂 ساختار پروژه (Project Structure)

پروژه به منظور تفکیک وظایف (Separation of Concerns) به پکیج‌های زیر تقسیم شده است:

* **`server`**: شامل منطق اصلی سرور، مدیریت نشست‌های کاربران (Sessions) و مدیریت اتاق‌های چت.
* **`client`**: شامل پیاده‌سازی سمت کاربر و دو ترد مجزا برای ارسال و دریافت همزمان داده‌ها.
* **`common`**: شامل مدل‌های داده‌ای مشترک مانند `Message` و ساختار `ExportData`.
* **`network`**: شامل پروتکل تبادل داده (Packet System) برای استانداردسازی ارتباطات.



---

## 🚀 نحوه اجرا (How to Run)

### ۱. کامپایل پروژه
ابتدا یک ترمینال در پوشه اصلی پروژه باز کرده و دستورات زیر را اجرا کنید:

```bash
# ایجاد پوشه برای فایل‌های کامپایل شده
mkdir bin

# کامپایل تمامی فایل‌های جاوا در پوشه bin
javac -d bin -sourcepath src src/server/*.java src/client/*.java src/common/*.java src/network/*.java
